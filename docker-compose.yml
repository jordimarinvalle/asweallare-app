version: '3.8'

services:
  # ============================================================================
  # UI App (Framework7 + Vite) - Consumer facing app
  # ============================================================================
  ui:
    build:
      context: ./apps/ui
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - VITE_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      - VITE_API_URL=http://admin:3001/api
    volumes:
      - ./apps/ui:/app
      - /app/node_modules
      - ./public:/app/public/assets

  # ============================================================================
  # Admin App (Next.js) - Admin panel
  # ============================================================================
  admin:
    build:
      context: ./apps/admin
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_BASE_URL=http://localhost:3001
      - NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY}
    volumes:
      - ./apps/admin:/app
      - /app/node_modules
      - /app/.next
      - ./public:/app/public

  # ============================================================================
  # LOCAL POSTGRESQL - COMMENTED OUT (Using Supabase instead)
  # Uncomment these sections if you need local database for offline development
  # ============================================================================
  
  # postgres:
  #   image: postgres:15-alpine
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: asweallare
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./database/schema-local.sql:/docker-entrypoint-initdb.d/01-schema.sql
  #     - ./database/fixtures.sql:/docker-entrypoint-initdb.d/02-fixtures.sql
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   ports:
  #     - "5050:80"
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@admin.com
  #     PGADMIN_DEFAULT_PASSWORD: admin
  #     PGADMIN_CONFIG_SERVER_MODE: 'False'
  #   volumes:
  #     - pgadmin_data:/var/lib/pgadmin
  #   depends_on:
  #     - postgres
  #   profiles:
  #     - tools

# volumes:
#   postgres_data:
#   pgadmin_data:
